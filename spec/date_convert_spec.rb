require 'spec_helper'

describe DateConvert do
  it 'has a version number' do
    expect(DateConvert::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
